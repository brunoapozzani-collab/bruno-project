# Tools

Deterministic Python scripts. Each script should:
- Load secrets from `.env` via `python-dotenv`
- Take inputs as CLI args or function params (no hardcoded paths)
- Print clear errors and exit non-zero on failure
- Be runnable standalone for testing
